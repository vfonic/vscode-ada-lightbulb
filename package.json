{
  "name": "ada-lightbulb",
  "displayName": "Ada Lightbulb",
  "description": "The only git GUI you'll ever need",
  "version": "0.1.0",
  "publisher": "vfonic",
  "repository": {
    "type": "git",
    "url": "https://github.com/vfonic/ada-lightbulb.git"
  },
  "engines": {
    "vscode": "^1.33.0"
  },
  "categories": [
    "SCM Providers"
  ],
  "activationEvents": [
    "*"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "commands": [
      {
        "category": "Ada Lightbulb",
        "command": "ada-lightbulb.view",
        "title": "Open graph"
      },
      {
        "category": "Ada Lightbulb",
        "command": "ada-lightbulb.clearAvatarCache",
        "title": "Clear Avatar Cache"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Ada Lightbulb",
      "properties": {
        "ada-lightbulb.autoCenterCommitDetailsView": {
          "type": "boolean",
          "default": true,
          "description": "Automatically center the commit details view when it is opened."
        },
        "ada-lightbulb.dateFormat": {
          "type": "string",
          "enum": [
            "Date & Time",
            "Date Only",
            "Relative"
          ],
          "enumDescriptions": [
            "Show the date and time, for example \"19 Mar 2019 21:34\"",
            "Show the date only, for example \"19 Mar 2019\"",
            "Show relative times, for example \"5 minutes ago\""
          ],
          "default": "Date & Time",
          "description": "Specifies the date format to be used in the date column of Ada Lightbulb."
        },
        "ada-lightbulb.dateType": {
          "type": "string",
          "enum": [
            "Author Date",
            "Commit Date"
          ],
          "enumDescriptions": [
            "Use the author date of a commit",
            "Use the committer date of a commit"
          ],
          "default": "Author Date",
          "description": "Specifies the date type to be displayed throughout Ada Lightbulb."
        },
        "ada-lightbulb.fetchAvatars": {
          "type": "boolean",
          "default": false,
          "description": "Fetch avatars of commit authors and committers. By enabling this setting, you consent to commit author and committer email addresses being sent GitHub, GitLab or Gravatar, depending on the repositories remote origin."
        },
        "ada-lightbulb.graphColours": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "Colour (HEX or RGB)",
            "pattern": "^\\s*(#[0-9a-fA-F]{6}|#[0-9a-fA-F]{8}|rgb[a]?\\s*\\(\\d{1,3},\\s*\\d{1,3},\\s*\\d{1,3}\\))\\s*$"
          },
          "default": [
            "#0085d9",
            "#d9008f",
            "#00d90a",
            "#d98500",
            "#a300d9",
            "#ff0000",
            "#00d9cc",
            "#e138e8",
            "#85d900",
            "#dc5b23",
            "#6f24d6",
            "#ffcc00"
          ],
          "description": "Specifies the colours used on the graph."
        },
        "ada-lightbulb.graphStyle": {
          "type": "string",
          "enum": [
            "rounded",
            "angular"
          ],
          "enumDescriptions": [
            "Use smooth curves when transitioning between branches on the graph",
            "Use angular lines when transitioning between branches on the graph"
          ],
          "default": "rounded",
          "description": "Specifies the style of the graph."
        },
        "ada-lightbulb.initialLoadCommits": {
          "type": "number",
          "default": 300,
          "description": "Specifies the number of commits to initially load."
        },
        "ada-lightbulb.loadMoreCommits": {
          "type": "number",
          "default": 100,
          "description": "Specifies the number of commits to load when the \"Load More Commits\" button is pressed (only shown when more commits are available)."
        },
        "ada-lightbulb.maxDepthOfRepoSearch": {
          "type": "number",
          "default": 0,
          "description": "Specifies the maximum depth of subfolders to search when discovering repositories in the workspace."
        },
        "ada-lightbulb.showCurrentBranchByDefault": {
          "type": "boolean",
          "default": false,
          "description": "Show the current branch by default when Ada Lightbulb is opened. Default: false (show all branches)"
        },
        "ada-lightbulb.showStatusBarItem": {
          "type": "boolean",
          "default": true,
          "description": "Show a Status Bar item which opens Ada Lightbulb when clicked."
        },
        "ada-lightbulb.showUncommittedChanges": {
          "type": "boolean",
          "default": true,
          "description": "Show uncommitted changes (set to false to decrease load time on large repositories)."
        },
        "ada-lightbulb.tabIconColourTheme": {
          "type": "string",
          "enum": [
            "colour",
            "grey"
          ],
          "enumDescriptions": [
            "Show a colour icon which suits most Visual Studio Code colour themes",
            "Show a grey icon which suits Visual Studio Code colour themes that are predominantly grayscale"
          ],
          "default": "colour",
          "description": "Specifies the colour theme of the icon displayed on the Ada Lightbulb tab."
        }
      }
    }
  },
  "scripts": {
    "package-and-install": "vsce package && node ./.vscode/install-package.js",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "node ./node_modules/vscode/bin/test"
  },
  "devDependencies": {
    "eslint": "^5.13.0",
    "eslint-config-prettier": "^4.2.0",
    "eslint-plugin-prettier": "^3.0.1",
    "prettier": "1.17.0",
    "vscode": "^1.1.28"
  }
}
